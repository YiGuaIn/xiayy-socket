const server = {
    SERVICE: 'http://localhost', // 服务地址
    HTTP_PORT: 8013, // http端口
    SOCKET_PORT: 8021, // websocket端口
    HTTP_SERVICE: `http://localhos:8013`, // Http 服务地址
    SOCKET_SERVICE: `http://localhos:8021` // Websocket 服务地址
};

module.exports = exports = server;